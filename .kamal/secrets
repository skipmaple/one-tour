# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# Example of extracting secrets from 1password (or another compatible pw manager)
# SECRETS=$(kamal secrets fetch --adapter 1password --account your-account --from Vault/Item KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY)
# KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD ${SECRETS})
# RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})

# Use a GITHUB_TOKEN if private repositories are needed for the image
# GITHUB_TOKEN=$(gh config get -h github.com oauth_token)

# Grab the registry password from .env.production
KAMAL_REGISTRY_PASSWORD=$(grep -m1 "^KAMAL_REGISTRY_PASSWORD=" .env.production | cut -d= -f2-)

# Improve security by using a password manager. Never check config/master.key into git!
RAILS_MASTER_KEY=$(cat config/master.key)

# Production secrets — read directly from .env.production (no need to source before deploying)
# .env is for development only; .env.production holds all production credentials.
POSTGRES_PASSWORD=$(grep -m1 "^POSTGRES_PASSWORD=" .env.production | cut -d= -f2-)
ONE_TOUR_DATABASE_PASSWORD=$(grep -m1 "^POSTGRES_PASSWORD=" .env.production | cut -d= -f2-)
GITHUB_CLIENT_ID=$(grep -m1 "^GITHUB_CLIENT_ID=" .env.production | cut -d= -f2-)
GITHUB_CLIENT_SECRET=$(grep -m1 "^GITHUB_CLIENT_SECRET=" .env.production | cut -d= -f2-)
GOOGLE_CLIENT_ID=$(grep -m1 "^GOOGLE_CLIENT_ID=" .env.production | cut -d= -f2-)
GOOGLE_CLIENT_SECRET=$(grep -m1 "^GOOGLE_CLIENT_SECRET=" .env.production | cut -d= -f2-)
FEISHU_APP_ID=$(grep -m1 "^FEISHU_APP_ID=" .env.production | cut -d= -f2-)
FEISHU_APP_SECRET=$(grep -m1 "^FEISHU_APP_SECRET=" .env.production | cut -d= -f2-)
OPENAI_API_KEY=$(grep -m1 "^OPENAI_API_KEY=" .env.production | cut -d= -f2-)
OPENAI_API_BASE=$(grep -m1 "^OPENAI_API_BASE=" .env.production | cut -d= -f2-)
LLM_MODEL=$(grep -m1 "^LLM_MODEL=" .env.production | cut -d= -f2-)
AMAP_API_KEY=$(grep -m1 "^AMAP_API_KEY=" .env.production | cut -d= -f2-)
GOOGLE_MAPS_API_KEY=$(grep -m1 "^GOOGLE_MAPS_API_KEY=" .env.production | cut -d= -f2-)
RESEND_API_KEY=$(grep -m1 "^RESEND_API_KEY=" .env.production | cut -d= -f2-)
MAIL_FROM=$(grep -m1 "^MAIL_FROM=" .env.production | cut -d= -f2-)
EMAIL_VERIFICATION_PEPPER=$(grep -m1 "^EMAIL_VERIFICATION_PEPPER=" .env.production | cut -d= -f2-)
R2_ACCESS_KEY_ID=$(grep -m1 "^R2_ACCESS_KEY_ID=" .env.production | cut -d= -f2-)
R2_SECRET_ACCESS_KEY=$(grep -m1 "^R2_SECRET_ACCESS_KEY=" .env.production | cut -d= -f2-)
R2_ENDPOINT=$(grep -m1 "^R2_ENDPOINT=" .env.production | cut -d= -f2-)
R2_BUCKET=$(grep -m1 "^R2_BUCKET=" .env.production | cut -d= -f2-)
