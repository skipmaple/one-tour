# Tranche D-2 — Drag validation + Undo + useChat history 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现拖拽 hover 超时预警、buffer day 二次确认、全局 undo stack（5 个 mutation）、useChat 刷新加载历史。

**Architecture:** 4 个独立子模块按依赖顺序交付。Undo 是纯前端 React Context (`useUndoStack` hook)，每个 mutation 调用点 onSuccess 内 push entry。Mantine `notifications.show` 的 `message` 接受 ReactNode → toast 内嵌"撤销"按钮 + Cmd+Z 全局快捷键。Backend 仅 ActivitiesController/DaysController#create 改 `respond_to` 加 JSON 返 id。

**Tech Stack:** React 19 (useReducer + Context), @dnd-kit/core (onDragOver), @mantine/notifications (ReactNode message + update API), @mantine/modals (openConfirmModal), Inertia router + native fetch (for create with id response), Vitest, RSpec.

**参考 spec:** `docs/superpowers/specs/2026-04-16-tranche-d2-design.md`

---

## 执行约定

- **TDD 严格**：每个有行为的 task 按 "先写测试 → 验证失败 → 实现 → 验证通过 → commit" 节奏。
- **常用命令**：
  - JS 测试：`npm test` 或 `npx vitest run <path>`
  - Ruby 测试：`mise exec -- bundle exec rspec [<path>]`
  - Rubocop：`mise exec -- bundle exec rubocop -f github`
- **commit 规范**：短 subject + 可选 body；不要 `--amend`，失败后新 commit。
- **手动 QA**：最终验证用 Tour #17（已在 dev DB 种好真实数据）。

---

## 文件结构

**新建：**
- `app/javascript/hooks/useUndoStack.jsx` — Context Provider + hook + Cmd+Z keybinding + showUndoToast helper
- `app/javascript/hooks/__tests__/useUndoStack.test.jsx` — Vitest, 6 条
- `app/javascript/hooks/__tests__/useChat.test.js` — Vitest 历史加载，2 条

**修改（前端）：**
- `app/javascript/hooks/useChat.js` — mount fetch + reducer load_history case
- `app/javascript/entrypoints/inertia.jsx` — 挂 `<UndoStackProvider>`
- `app/javascript/pages/Tour/Show.jsx` — dragWarning state + onDragOver + buffer modal + performMove 提取 + push undo
- `app/javascript/pages/Tour/__tests__/Show.test.jsx` — buffer modal 触发测试 + dragWarning 测试
- `app/javascript/components/planner/DayColumn.jsx` — dragWarning prop + 红框 + warning banner
- `app/javascript/components/planner/__tests__/DayColumn.test.jsx`（如不存在则新建）— dragWarning 渲染测试
- `app/javascript/components/activity-editor/ActivityDrawer.jsx` — create 改 fetch + 3 处 push undo
- `app/javascript/components/activity-editor/__tests__/ActivityDrawer.test.jsx` — fetch mock + undo push 断言
- `app/javascript/components/planner/DayEditModal.jsx` — update 处 push undo
- `app/javascript/components/planner/__tests__/DayEditModal.test.jsx` — undo push 断言

**修改（后端）：**
- `app/controllers/activities_controller.rb` — `create` 用 `respond_to` 返 JSON 含 id
- `app/controllers/days_controller.rb` — `create` 同
- `app/models/message.rb` — `as_json` 覆盖让 role 输出字符串而非 integer（如未已经）
- `spec/requests/activities_spec.rb` — JSON create 返 id 断言
- `spec/requests/days_spec.rb` — 同
- `spec/models/message_spec.rb`（如未存在则新建）— role 序列化测试

---

## 路线图

| Task | 主题 | 结束后能做什么 |
|---|---|---|
| 1 | Ops-A · useChat 历史加载（含 Message#as_json 修） | 刷新 Chat 后看到历史 transcript |
| 2 | Backend controller create 改返 JSON 含 id | 前端可拿新建对象 id |
| 3 | useUndoStack hook + Provider + Cmd+Z + showUndoToast | undo 基础设施可用，但还无消费者 |
| 4 | G5-a · hover 超时预警 | 拖到超时 day 时实时变红 |
| 5 | G5-b · buffer day 二次确认 | 拖到 buffer day 弹 modal，确认后联动改 buffer_day |
| 6 | 5 个 mutation 调用点接入 undoStack.push | drag/create/update/delete activity + update day 都能撤销 |
| 7 | 最终验证 + 浏览器手动 QA | 全部通过 |

---

## Task 1: Ops-A · useChat 历史加载

**Files:**
- Modify: `app/javascript/hooks/useChat.js`
- Modify: `app/models/message.rb`
- Create: `app/javascript/hooks/__tests__/useChat.test.js`
- Create: `spec/models/message_spec.rb`（如未存在）

### 1A · Backend: Message#as_json 序列化 role 为字符串

- [ ] **Step 1: 检查 Message 是否有 spec 文件**

Run: `ls spec/models/message_spec.rb`

如果存在，下面 step 2-4 在该文件追加；不存在则按 step 2 创建。

- [ ] **Step 2: 写失败 spec**

创建或追加 `spec/models/message_spec.rb`：

```ruby
require "rails_helper"

RSpec.describe Message do
  describe "#as_json" do
    let(:conversation) { create(:conversation) }

    it "serializes role as string ('user'), not integer" do
      msg = conversation.messages.create!(role: :user, content: "hi")
      expect(msg.as_json["role"]).to eq("user")
    end

    it "serializes role 'assistant' as string" do
      msg = conversation.messages.create!(role: :assistant, content: "ok")
      expect(msg.as_json["role"]).to eq("assistant")
    end
  end
end
```

- [ ] **Step 3: 验证失败**

Run: `mise exec -- bundle exec rspec spec/models/message_spec.rb -e "as_json" --format documentation`

Expected: FAIL — `expected: "user", got: 0`（Rails enum 默认序列化为 integer）

- [ ] **Step 4: 实现**

修改 `app/models/message.rb`：

```ruby
class Message < ApplicationRecord
  belongs_to :conversation

  enum :role, { user: 0, assistant: 1, system: 2, tool: 3 }

  # Override as_json so role is exposed as the enum key (string),
  # not the underlying integer. Frontend (useChat / MessageBubble)
  # reads `role` as 'user' / 'assistant' to render bubbles correctly.
  def as_json(options = nil)
    super.tap do |hash|
      hash["role"] = role if hash.key?("role")
    end
  end
end
```

- [ ] **Step 5: 验证通过**

Run: `mise exec -- bundle exec rspec spec/models/message_spec.rb --format documentation`

Expected: 2 PASS

### 1B · Frontend: useChat mount fetch

- [ ] **Step 6: 创建 useChat 测试文件**

创建 `app/javascript/hooks/__tests__/useChat.test.js`：

```js
import { describe, test, expect, vi, beforeEach } from 'vitest'
import { reducer, INITIAL } from '../useChat'

describe('useChat reducer', () => {
  test('load_history replaces messages with fetched history', () => {
    const next = reducer(INITIAL, {
      type: 'load_history',
      messages: [
        { role: 'user', content: 'hi' },
        { role: 'assistant', content: 'hello' }
      ]
    })
    expect(next.messages).toHaveLength(2)
    expect(next.messages[0].content).toBe('hi')
    expect(next.messages[1].role).toBe('assistant')
  })

  test('load_history with empty array keeps state shape valid', () => {
    const next = reducer(INITIAL, { type: 'load_history', messages: [] })
    expect(next.messages).toEqual([])
    expect(next.streaming).toBe(false)
  })
})
```

- [ ] **Step 7: 验证失败**

Run: `npx vitest run app/javascript/hooks/__tests__/useChat.test.js`

Expected: FAIL — `load_history` action 不存在，reducer 走 default 返回原 state，`messages` 仍是 `[]` 不是 fixture。

- [ ] **Step 8: 修改 useChat.js 加 load_history reducer case + mount fetch**

在 `app/javascript/hooks/useChat.js` 的 `reducer` switch 里，在 `case 'error':` 之前插入：

```js
    case 'load_history':
      return { ...state, messages: action.messages }

```

然后修改 `useChat` default export 函数，在现有的 `useEffect [tourId]`（订阅 ActionCable 那个）**之前**插入新的 mount fetch effect：

```jsx
  useEffect(() => {
    if (!tourId) return
    fetch(`/tours/${tourId}/conversation`, {
      headers: { Accept: 'application/json' }
    })
      .then(res => res.ok ? res.json() : null)
      .then(data => {
        if (data?.messages) {
          dispatch({ type: 'load_history', messages: data.messages })
        }
      })
      .catch(() => { /* silent — chat starts empty on error */ })
  }, [ tourId ])

```

- [ ] **Step 9: 验证 reducer 测试通过**

Run: `npx vitest run app/javascript/hooks/__tests__/useChat.test.js`

Expected: 2 PASS

- [ ] **Step 10: 全量 Vitest + RSpec 确认无回归**

Run: `npm test`
Expected: 全 PASS

Run: `mise exec -- bundle exec rspec --format progress`
Expected: 全 PASS

- [ ] **Step 11: Commit**

```bash
git add app/javascript/hooks/useChat.js app/javascript/hooks/__tests__/useChat.test.js app/models/message.rb spec/models/message_spec.rb
git commit -m "chat: useChat loads history on mount + Message#as_json role as string"
```

---

## Task 2: Backend controller create 返 JSON 含 id

**Files:**
- Modify: `app/controllers/activities_controller.rb`
- Modify: `app/controllers/days_controller.rb`
- Modify: `spec/requests/activities_spec.rb`
- Modify: `spec/requests/days_spec.rb`

### 2A · ActivitiesController

- [ ] **Step 1: 写失败 spec**

在 `spec/requests/activities_spec.rb` 末尾（最后一个 `end` 之前）追加：

```ruby
  describe "POST create with Accept: application/json" do
    it "returns id and position in JSON for day-scoped create" do
      day = create(:day, tour: tour, day_index: 2)
      login_as(author)
      post tour_day_activities_path(tour, day),
        params: { activity: { name: "新景点", kind: "scenic", citizen_level: "tier_one" } },
        headers: { "Accept" => "application/json" }
      expect(response).to have_http_status(:ok)
      body = JSON.parse(response.body)
      expect(body["id"]).to be_a(Integer)
      expect(body["position"]).to be_a(Integer)
    end

    it "returns id for backlog create" do
      login_as(author)
      post tour_backlog_activities_path(tour),
        params: { activity: { name: "待选", kind: "scenic", citizen_level: "tier_three" } },
        headers: { "Accept" => "application/json" }
      expect(response).to have_http_status(:ok)
      body = JSON.parse(response.body)
      expect(body["id"]).to be_a(Integer)
    end
  end
```

- [ ] **Step 2: 验证失败**

Run: `mise exec -- bundle exec rspec spec/requests/activities_spec.rb -e "Accept: application/json" --format documentation`

Expected: FAIL — controller 现在 `redirect_to tour`，response 是 302 不是 200 + JSON。

- [ ] **Step 3: 实现 ActivitiesController#create 用 respond_to**

替换 `app/controllers/activities_controller.rb` 的 `create` action：

```ruby
  def create
    if params[:day_id]
      day = Day.find(params[:day_id])
      tour = day.tour
      head :forbidden and return unless tour.editable_by?(current_user)
      @activity = tour.activities.create!(activity_params.merge(day: day, position: next_position(tour, day)))
    else
      tour = Tour.find(params[:tour_id])
      head :forbidden and return unless tour.editable_by?(current_user)
      @activity = tour.activities.create!(activity_params.merge(day: nil, position: next_position(tour, nil)))
    end
    respond_to do |format|
      format.json { render json: { id: @activity.id, position: @activity.position } }
      format.html { redirect_to @activity.tour }
    end
  end
```

注意：`@activity` 现在赋值供 respond_to 用；其它 actions 不变。

- [ ] **Step 4: 验证通过**

Run: `mise exec -- bundle exec rspec spec/requests/activities_spec.rb --format documentation`

Expected: 全 PASS（含 2 条新 + 4 条已有）

### 2B · DaysController

- [ ] **Step 5: 写失败 spec**

在 `spec/requests/days_spec.rb` 末尾追加：

```ruby
  describe "POST create with Accept: application/json" do
    it "returns id in JSON" do
      tour = create(:tour, author: author)
      login_as(author)
      post tour_days_path(tour),
        params: { day: { day_index: 2 } },
        headers: { "Accept" => "application/json" }
      expect(response).to have_http_status(:ok)
      body = JSON.parse(response.body)
      expect(body["id"]).to be_a(Integer)
    end
  end
```

如果文件没 `let(:author)` 或 `login_as`，先看 spec 头部模式参照已有结构调整。

- [ ] **Step 6: 验证失败**

Run: `mise exec -- bundle exec rspec spec/requests/days_spec.rb -e "Accept: application/json" --format documentation`

Expected: FAIL

- [ ] **Step 7: 实现**

替换 `app/controllers/days_controller.rb` 的 `create` action：

```ruby
  def create
    @day = @tour.days.create!(day_params)
    respond_to do |format|
      format.json { render json: { id: @day.id } }
      format.html { redirect_to @tour }
    end
  end
```

- [ ] **Step 8: 验证通过**

Run: `mise exec -- bundle exec rspec spec/requests/days_spec.rb --format documentation`

Expected: 全 PASS

- [ ] **Step 9: 全量 RSpec 确认无回归**

Run: `mise exec -- bundle exec rspec --format progress`
Expected: 全 PASS

- [ ] **Step 10: Commit**

```bash
git add app/controllers/activities_controller.rb app/controllers/days_controller.rb spec/requests/activities_spec.rb spec/requests/days_spec.rb
git commit -m "controller: ActivitiesController/DaysController#create return JSON with id"
```

---

## Task 3: useUndoStack hook + Provider + Cmd+Z + showUndoToast

**Files:**
- Create: `app/javascript/hooks/useUndoStack.jsx`
- Create: `app/javascript/hooks/__tests__/useUndoStack.test.jsx`
- Modify: `app/javascript/entrypoints/inertia.jsx`

- [ ] **Step 1: 创建测试文件**

创建 `app/javascript/hooks/__tests__/useUndoStack.test.jsx`：

```jsx
import { describe, test, expect, vi, beforeEach, afterEach } from 'vitest'
import { renderHook, act } from '@testing-library/react'
import { MantineProvider } from '@mantine/core'
import { Notifications } from '@mantine/notifications'
import { UndoStackProvider, useUndoStack, UNDO_CAP } from '../useUndoStack'

// Mantine notifications uses portals; need full provider tree
function wrapper({ children }) {
  return (
    <MantineProvider>
      <Notifications />
      <UndoStackProvider>{children}</UndoStackProvider>
    </MantineProvider>
  )
}

describe('useUndoStack', () => {
  test('push appends entry to stack', () => {
    const { result } = renderHook(() => useUndoStack(), { wrapper })
    act(() => {
      result.current.push({ label: 'A', undoFn: () => Promise.resolve() })
    })
    expect(result.current.stack).toHaveLength(1)
    expect(result.current.stack[0].label).toBe('A')
  })

  test('cap=10: pushing 11th shifts oldest out', () => {
    const { result } = renderHook(() => useUndoStack(), { wrapper })
    act(() => {
      for (let i = 0; i < 11; i++) {
        result.current.push({ label: `entry-${i}`, undoFn: () => Promise.resolve() })
      }
    })
    expect(result.current.stack).toHaveLength(UNDO_CAP)
    expect(result.current.stack[0].label).toBe('entry-1') // oldest dropped
    expect(result.current.stack[UNDO_CAP - 1].label).toBe('entry-10')
  })

  test('UNDO_CAP equals 10', () => {
    expect(UNDO_CAP).toBe(10)
  })

  test('executeTop pops top and calls undoFn', async () => {
    const undoFn = vi.fn(() => Promise.resolve())
    const { result } = renderHook(() => useUndoStack(), { wrapper })
    act(() => {
      result.current.push({ label: 'A', undoFn })
    })
    await act(async () => {
      await result.current.executeTop()
    })
    expect(undoFn).toHaveBeenCalledOnce()
    expect(result.current.stack).toHaveLength(0)
  })

  test('executeTop is no-op on empty stack', async () => {
    const { result } = renderHook(() => useUndoStack(), { wrapper })
    await act(async () => {
      const ret = await result.current.executeTop()
      expect(ret).toBeUndefined()
    })
    expect(result.current.stack).toHaveLength(0)
  })

  test('Cmd+Z keydown triggers executeTop on the top entry', async () => {
    const undoFn = vi.fn(() => Promise.resolve())
    const { result } = renderHook(() => useUndoStack(), { wrapper })
    act(() => {
      result.current.push({ label: 'A', undoFn })
    })
    await act(async () => {
      const event = new KeyboardEvent('keydown', { key: 'z', metaKey: true, bubbles: true, cancelable: true })
      window.dispatchEvent(event)
      // Wait for async undoFn
      await new Promise(r => setTimeout(r, 0))
    })
    expect(undoFn).toHaveBeenCalledOnce()
  })

  test('Cmd+Z is ignored when focus is in a textarea', async () => {
    const undoFn = vi.fn(() => Promise.resolve())
    const { result } = renderHook(() => useUndoStack(), { wrapper })
    act(() => {
      result.current.push({ label: 'A', undoFn })
    })
    // Create + focus a textarea
    const ta = document.createElement('textarea')
    document.body.appendChild(ta)
    ta.focus()
    await act(async () => {
      const event = new KeyboardEvent('keydown', { key: 'z', metaKey: true, bubbles: true, cancelable: true })
      ta.dispatchEvent(event)
      await new Promise(r => setTimeout(r, 0))
    })
    expect(undoFn).not.toHaveBeenCalled()
    document.body.removeChild(ta)
  })
})
```

- [ ] **Step 2: 验证失败**

Run: `npx vitest run app/javascript/hooks/__tests__/useUndoStack.test.jsx`

Expected: FAIL — useUndoStack 不存在

- [ ] **Step 3: 创建 useUndoStack.jsx**

创建 `app/javascript/hooks/useUndoStack.jsx`：

```jsx
import { createContext, useContext, useState, useEffect, useCallback, useRef } from 'react'
import { notifications } from '@mantine/notifications'
import { Group, Text, Button } from '@mantine/core'

// In-memory undo stack capped at 10. Refresh clears it.
// Each entry: { label: string, undoFn: () => Promise<void>, ts: number }
export const UNDO_CAP = 10

const UndoStackContext = createContext(null)

export function UndoStackProvider({ children }) {
  const [ stack, setStack ] = useState([])
  const stackRef = useRef(stack)
  stackRef.current = stack

  const push = useCallback(({ label, undoFn }) => {
    const entry = { label, undoFn, ts: Date.now() + Math.random() }
    setStack(prev => {
      const next = [ ...prev, entry ]
      if (next.length > UNDO_CAP) next.shift()
      return next
    })
    showUndoToast({ entry, executeUndo: () => executeEntry(entry) })
    // executeEntry needs to be defined; we'll use a closure that reads stackRef
  }, [])

  // Executes a specific entry's undoFn and removes it from stack.
  // Wrapped in useCallback so toast button references stay stable.
  const executeEntry = useCallback(async (entry) => {
    setStack(prev => prev.filter(e => e.ts !== entry.ts))
    try {
      await entry.undoFn()
    } catch (err) {
      throw err
    }
  }, [])

  const executeTop = useCallback(async () => {
    const top = stackRef.current[stackRef.current.length - 1]
    if (!top) return undefined
    await executeEntry(top)
  }, [ executeEntry ])

  // Global Cmd+Z / Ctrl+Z keybinding. Skip when focus is in editable element.
  useEffect(() => {
    function handleKeydown(e) {
      if (!(e.metaKey || e.ctrlKey)) return
      if (e.key !== 'z' || e.shiftKey) return
      const tag = e.target.tagName?.toLowerCase()
      if (tag === 'input' || tag === 'textarea' || e.target.isContentEditable) return
      e.preventDefault()
      executeTop()
    }
    window.addEventListener('keydown', handleKeydown)
    return () => window.removeEventListener('keydown', handleKeydown)
  }, [ executeTop ])

  const value = { push, executeTop, stack }
  return <UndoStackContext.Provider value={value}>{children}</UndoStackContext.Provider>
}

export function useUndoStack() {
  const ctx = useContext(UndoStackContext)
  if (!ctx) throw new Error('useUndoStack must be used within UndoStackProvider')
  return ctx
}

// Show a Mantine toast with embedded "撤销" button.
// Uses notifications.update to transition to in-progress / done / error states.
function showUndoToast({ entry, executeUndo }) {
  const id = `undo-${entry.ts}`
  notifications.show({
    id,
    autoClose: 5000,
    withCloseButton: true,
    message: (
      <Group gap="xs" justify="space-between" wrap="nowrap">
        <Text size="sm">{entry.label}</Text>
        <Button
          variant="subtle"
          size="compact-xs"
          onClick={() => {
            notifications.update({
              id, loading: true, autoClose: false,
              message: <Text size="sm">撤销中…</Text>,
            })
            executeUndo()
              .then(() => notifications.update({
                id, loading: false, color: 'green', autoClose: 1500,
                message: <Text size="sm">{entry.label} · 已撤销</Text>,
              }))
              .catch(err => notifications.update({
                id, loading: false, color: 'red', autoClose: 3000,
                message: <Text size="sm">撤销失败：{err.message || '服务器拒绝'}</Text>,
              }))
          }}
        >
          撤销
        </Button>
      </Group>
    ),
  })
  return id
}
```

注意 — push 内部直接调 showUndoToast，executeUndo 用 entry 闭包；executeEntry 在 closure scope 同一个 Provider 内调用，是没问题的因为 UndoStackProvider 只挂载一次（全局）。

- [ ] **Step 4: 验证测试通过**

Run: `npx vitest run app/javascript/hooks/__tests__/useUndoStack.test.jsx`

Expected: 7 PASS

- [ ] **Step 5: 挂 UndoStackProvider 到 inertia.jsx**

修改 `app/javascript/entrypoints/inertia.jsx`：

在 imports 区加：

```jsx
import { UndoStackProvider } from '../hooks/useUndoStack'
```

修改 `setup` 内的 createRoot：把 `<App {...props} />` 用 `<UndoStackProvider>` 包起来。最终 JSX 应该是：

```jsx
    createRoot(el).render(
      <MantineProvider theme={theme}>
        <DatesProvider settings={{ locale: 'zh-cn', firstDayOfWeek: 1 }}>
          <ModalsProvider>
            <Notifications />
            <UndoStackProvider>
              <App {...props} />
            </UndoStackProvider>
          </ModalsProvider>
        </DatesProvider>
      </MantineProvider>
    )
```

- [ ] **Step 6: 全量 Vitest 确认无回归**

Run: `npm test`

Expected: 全 PASS

如果某些 Show.test.jsx 之类的旧测试因为 useUndoStack Provider 缺失报错，加 wrapper 即可。但 useUndoStack 只在 Show.jsx 等具体调用方使用——而 Show.test.jsx 里 mock 了大部分依赖，这一步若 fail，是 Show.jsx 已经引入了 useUndoStack 而 mock 没加。本 task 不引入 useUndoStack 的消费者（Task 6 才接），所以应无影响。

- [ ] **Step 7: Commit**

```bash
git add app/javascript/hooks/useUndoStack.jsx app/javascript/hooks/__tests__/useUndoStack.test.jsx app/javascript/entrypoints/inertia.jsx
git commit -m "hooks: useUndoStack with toast button + Cmd+Z global keybinding"
```

---

## Task 4: G5-a · Hover 超时预警

**Files:**
- Modify: `app/javascript/pages/Tour/Show.jsx`
- Modify: `app/javascript/pages/Tour/__tests__/Show.test.jsx`
- Modify: `app/javascript/components/planner/DayColumn.jsx`
- Create or modify: `app/javascript/components/planner/__tests__/DayColumn.test.jsx`

### 4A · DayColumn 接 dragWarning prop

- [ ] **Step 1: 检查 DayColumn 测试是否存在**

Run: `ls app/javascript/components/planner/__tests__/DayColumn.test.jsx`

如果不存在则下面 step 2 创建文件，存在则在末尾追加。

- [ ] **Step 2: 写失败 spec（DayColumn dragWarning 渲染测试）**

在 `app/javascript/components/planner/__tests__/DayColumn.test.jsx` 追加（如新文件，前置 imports 看 BacklogList.test.jsx 模板）：

```jsx
import { describe, test, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MantineProvider } from '@mantine/core'
import { DndContext } from '@dnd-kit/core'
import DayColumn from '../DayColumn'

function renderIt(props = {}) {
  const day = { id: 10, day_index: 1, intensity_derived: 'green' }
  return render(
    <MantineProvider>
      <DndContext>
        <DayColumn day={day} activities={[]} constitution={{ max_daily_driving_minutes: 420 }} {...props} />
      </DndContext>
    </MantineProvider>
  )
}

describe('DayColumn dragWarning', () => {
  test('renders warning banner when dragWarning prop is set', () => {
    renderIt({ dragWarning: { current: 360, incoming: 180, limit: 420, total: 540 } })
    expect(screen.getByText(/加入后驾驶/)).toBeInTheDocument()
    expect(screen.getByText(/540\/420/)).toBeInTheDocument()
  })

  test('does not render warning banner when dragWarning is null', () => {
    renderIt({ dragWarning: null })
    expect(screen.queryByText(/加入后驾驶/)).not.toBeInTheDocument()
  })
})
```

如果文件本来就有 imports，确保 `vi`/`describe`/`test`/`expect`/`render`/`screen` 都已导入；否则按 BacklogList.test.jsx 风格补全。

- [ ] **Step 3: 验证失败**

Run: `npx vitest run app/javascript/components/planner/__tests__/DayColumn.test.jsx`

Expected: FAIL

- [ ] **Step 4: 改 DayColumn.jsx 接 dragWarning**

读 `app/javascript/components/planner/DayColumn.jsx` 确认现有结构。修改组件签名：

```jsx
export default function DayColumn({ day, activities, constitution, onAddActivity, onEditActivity, onEditDay, readOnly, dragWarning }) {
```

在 header 和 body 之间插入 warning banner（找到 "+ 加一个" 按钮之前）：

```jsx
      {dragWarning && (
        <div style={{
          padding: '4px 8px',
          background: '#fef0f0',
          border: '1px solid #c33',
          color: '#c33',
          fontSize: 11
        }}>
          ⚠ 加入后驾驶 {Math.round(dragWarning.total)}/{dragWarning.limit} min
        </div>
      )}
```

并修改 body Stack 的 style，加红边框条件：

```jsx
      <Stack gap={4} p="xs" ref={setNodeRef} style={{
        flex: 1, minHeight: 140,
        background: isOver ? '#f0f7ff' : undefined,
        border: dragWarning ? '1px solid var(--mantine-color-red-6)' : undefined
      }}>
```

- [ ] **Step 5: 验证 DayColumn 测试通过**

Run: `npx vitest run app/javascript/components/planner/__tests__/DayColumn.test.jsx`

Expected: 2 PASS

### 4B · Show.jsx 加 dragWarning state + onDragOver

- [ ] **Step 6: 改 Show.jsx 加 state + handler**

读 `app/javascript/pages/Tour/Show.jsx` 找到 `const [activeId, setActiveId] = useState(null)` 这行，紧跟其后添加：

```jsx
  const [ dragWarning, setDragWarning ] = useState(null)
```

找到 `function handleDragEnd` 的定义之前（任何位置都行，建议同区域放）添加：

```jsx
  function updateDragWarning(active, over) {
    if (!over) { setDragWarning(null); return }
    const targetDayId = over.data.current?.dayId
    if (!targetDayId) { setDragWarning(null); return }

    const activityId = Number(String(active.id).replace(/^activity-/, ''))
    const draggedActivity = displayActivities.find(a => a.id === activityId)
    if (!draggedActivity) { setDragWarning(null); return }

    if (draggedActivity.day_id === targetDayId) { setDragWarning(null); return }

    const targetDayActs = displayActivities.filter(a => a.day_id === targetDayId)
    const currentDriveMin = targetDayActs.reduce((sum, a) =>
      sum + (parseInt(a.details?.drive_min || 0, 10) || 0), 0)
    const incomingDriveMin = parseInt(draggedActivity.details?.drive_min || 0, 10) || 0
    const total = currentDriveMin + incomingDriveMin
    const limit = tour.constitution?.max_daily_driving_minutes || 420

    if (total > limit) {
      setDragWarning({ dayId: targetDayId, current: currentDriveMin, incoming: incomingDriveMin, limit, total })
    } else {
      setDragWarning(null)
    }
  }
```

修改 `<DndContext>` props，加 `onDragOver` + 在 `onDragEnd` / `onDragCancel` 里清 dragWarning。当前 DndContext 大概长这样：

```jsx
<DndContext
  collisionDetection={closestCenter}
  onDragStart={({ active }) => setActiveId(active.id)}
  onDragEnd={(e) => { setActiveId(null); handleDragEnd(e) }}
  onDragCancel={() => setActiveId(null)}
  autoScroll={{ acceleration: 10, threshold: { x: 0.15, y: 0.15 } }}
>
```

改为：

```jsx
<DndContext
  collisionDetection={closestCenter}
  onDragStart={({ active }) => setActiveId(active.id)}
  onDragOver={({ active, over }) => updateDragWarning(active, over)}
  onDragEnd={(e) => { setActiveId(null); setDragWarning(null); handleDragEnd(e) }}
  onDragCancel={() => { setActiveId(null); setDragWarning(null) }}
  autoScroll={{ acceleration: 10, threshold: { x: 0.15, y: 0.15 } }}
>
```

修改渲染 DayColumn 的位置，加 `dragWarning` prop：

```jsx
{days.map(d => (
  <DayColumn
    key={d.id}
    day={d}
    activities={byDay[d.id] || []}
    constitution={tour.constitution}
    onAddActivity={canEdit ? openCreate : undefined}
    onEditActivity={canEdit ? openEdit : undefined}
    onEditDay={canEdit ? setEditingDayId : undefined}
    readOnly={!canEdit}
    dragWarning={dragWarning?.dayId === d.id ? dragWarning : null}
  />
))}
```

- [ ] **Step 7: 加 Show.test.jsx dragWarning 拦截测试**

在 `app/javascript/pages/Tour/__tests__/Show.test.jsx` 的 ChatPanel mock 之后，加 mock `@dnd-kit/core`（如果还没有）让 onDragOver 可被访问。如已 mock 则 update mock 加上 onDragOver 暴露：

```jsx
const dndProps = { onDragStart: undefined, onDragOver: undefined, onDragEnd: undefined, onDragCancel: undefined }
vi.mock('@dnd-kit/core', () => ({
  DndContext: (props) => {
    Object.assign(dndProps, props)
    return <div>{props.children}</div>
  },
  DragOverlay: () => null,
  closestCenter: () => null,
  useDraggable: () => ({ attributes: {}, listeners: {}, setNodeRef: () => {}, setActivatorNodeRef: () => {}, isDragging: false }),
  useDroppable: () => ({ setNodeRef: () => {}, isOver: false }),
}))
```

如果 Show.test.jsx 已经有 `@dnd-kit/core` 的 mock，merge 上面的 `dndProps` 捕获；不要重复 mock。

测试本任务的具体行为往往复杂（需要触发 onDragOver、断言子组件 prop），更轻量做法是单独单测 `updateDragWarning` 这个纯函数。但 Show.jsx 内部函数不导出。

**实用近似**：本 task 跳过 Show.test.jsx 端到端测试，只确保 DayColumn dragWarning 渲染（已在 4A）+ Show.test.jsx 现有断言都通过。手动 QA 在 Task 7 验证 Show.jsx 正确联动。

- [ ] **Step 8: 全量 Vitest**

Run: `npm test`

Expected: 全 PASS（135+ tests）

- [ ] **Step 9: Commit**

```bash
git add app/javascript/pages/Tour/Show.jsx app/javascript/components/planner/DayColumn.jsx app/javascript/components/planner/__tests__/DayColumn.test.jsx
git commit -m "drag: hover-time driving overflow warning on DayColumn"
```

---

## Task 5: G5-b · Buffer day 二次确认

**Files:**
- Modify: `app/javascript/pages/Tour/Show.jsx`
- Modify: `app/javascript/pages/Tour/__tests__/Show.test.jsx`

- [ ] **Step 1: 读 Show.jsx handleDragEnd 当前结构**

Read file. handleDragEnd 当前体内大致是：

```jsx
function handleDragEnd({ active, over }) {
  if (!over) return
  if (active.id === over.id) return
  const activityId = Number(String(active.id).replace(/^activity-/, ''))
  const data = over.data.current || {}
  const toDayId = data.dayId ?? null
  const toPosition = data.position ?? 1

  // optimistic + router.patch
  ...
}
```

我们要在头部加 buffer 检测分支，并把现有 router.patch 逻辑提取成 `performMove(activityId, toDayId, toPosition)`。

- [ ] **Step 2: 改 handleDragEnd 加 buffer 拦截 + 提取 performMove**

把现有 `function handleDragEnd` 改为：

```jsx
function handleDragEnd({ active, over }) {
  if (!over) return
  if (active.id === over.id) return
  const activityId = Number(String(active.id).replace(/^activity-/, ''))
  const data = over.data.current || {}
  const toDayId = data.dayId ?? null
  const toPosition = data.position ?? 1

  const targetDay = toDayId ? days.find(d => d.id === toDayId) : null
  if (targetDay?.buffer_day) {
    modals.openConfirmModal({
      title: '把 activity 放进机动日？',
      children: (
        <Text size="sm">
          D{targetDay.day_index} 是机动日（缓冲）。继续放入会让 D{targetDay.day_index} 不再是机动日，确认吗？
        </Text>
      ),
      labels: { confirm: '继续放入', cancel: '取消' },
      confirmProps: { color: 'orange' },
      onConfirm: () => {
        router.patch(`/tours/${tour.id}/days/${toDayId}`, { day: { buffer_day: false } }, {
          preserveScroll: true,
          only: ['days', 'violations'],
          onSuccess: () => performMove(activityId, toDayId, toPosition),
          onError: () => notifications.show({ message: '修改 buffer 失败，未拖动', color: 'red' })
        })
      }
    })
    return
  }

  performMove(activityId, toDayId, toPosition)
}

function performMove(activityId, toDayId, toPosition) {
  // Optimistic: apply locally
  setLocalOverrides(prev => ({
    ...prev,
    [activityId]: { day_id: toDayId, position: toPosition }
  }))

  router.patch(
    `/activities/${activityId}/position`,
    { to_day_id: toDayId, to_position: toPosition },
    {
      preserveState: true,
      preserveScroll: true,
      only: [ 'activities', 'violations' ],
      onSuccess: () => {
        setLocalOverrides(prev => {
          const next = { ...prev }
          delete next[activityId]
          return next
        })
      },
      onError: () => {
        setLocalOverrides(prev => {
          const next = { ...prev }
          delete next[activityId]
          return next
        })
        notifications.show({ message: '拖拽未保存，请重试', color: 'red' })
      }
    }
  )
}
```

如果当前 setLocalOverrides 用的是不同的 cleanup 写法（比如 destructuring rest），保留原写法只是把整段移动到 performMove 即可。

`modals.openConfirmModal` 已在 Tranche A/B 有用过（DayEditModal delete 那里），import 应已在 Show.jsx；如未，加：

```jsx
import { modals } from '@mantine/modals'
```

`Text` 同样需 import 自 `@mantine/core`，应已有。

- [ ] **Step 3: 跑全量 Vitest**

Run: `npm test`

Expected: 全 PASS（行为没变，只是结构重排+加了 buffer 分支；buffer 分支测试在 step 4）

- [ ] **Step 4: 加 buffer 触发 modal 的 Show.test.jsx 测试**

测试需要 mock `@mantine/modals` 让 openConfirmModal 可被检测：

在 `app/javascript/pages/Tour/__tests__/Show.test.jsx` 的 mock 区追加（如未有）：

```jsx
import { vi } from 'vitest'
const mockOpenConfirmModal = vi.fn()
vi.mock('@mantine/modals', () => ({
  modals: {
    openConfirmModal: (...args) => mockOpenConfirmModal(...args),
    openContextModal: () => {},
    openModal: () => {},
    closeAll: () => {},
  },
  ModalsProvider: ({ children }) => children,
}))
```

注意：原 spec 可能已 mock @mantine/modals 用于 ActivityDrawer / DayEditModal 测试。如果已 mock，添加 `openConfirmModal: vi.fn()` 暴露 + reset。

由于 handleDragEnd 内 mock @dnd-kit/core 不容易触发完整流程，本 step 跳过——手动 QA 在 Task 7 验证。**只需保证现有 Show.test.jsx 的 N 条测试都仍通过。**

Run: `npm test`

Expected: 全 PASS

- [ ] **Step 5: Commit**

```bash
git add app/javascript/pages/Tour/Show.jsx app/javascript/pages/Tour/__tests__/Show.test.jsx
git commit -m "drag: buffer_day drop confirm modal + performMove extraction"
```

---

## Task 6: 5 个 mutation 调用点接入 undoStack

**Files:**
- Modify: `app/javascript/pages/Tour/Show.jsx` — performMove push undo
- Modify: `app/javascript/components/activity-editor/ActivityDrawer.jsx` — create 改 fetch + 3 处 push undo
- Modify: `app/javascript/components/activity-editor/__tests__/ActivityDrawer.test.jsx` — 调整 mock + push 断言
- Modify: `app/javascript/components/planner/DayEditModal.jsx` — update 处 push undo
- Modify: `app/javascript/components/planner/__tests__/DayEditModal.test.jsx` — push 断言

### 6A · Show.performMove 接 undo

- [ ] **Step 1: Show.jsx 内 import + 用 hook**

在 `app/javascript/pages/Tour/Show.jsx` imports 加：

```jsx
import { useUndoStack } from '../../hooks/useUndoStack'
```

在 Show 组件函数内（紧跟其它 useState 之后）：

```jsx
  const undoStack = useUndoStack()
```

修改 performMove，在调用 router.patch 之前快照 prevAttrs，在 onSuccess 内 push undo：

```jsx
function performMove(activityId, toDayId, toPosition) {
  // Snapshot prev position for undo
  const draggedActivity = displayActivities.find(a => a.id === activityId)
  const prevDayId = draggedActivity?.day_id ?? null
  const prevPosition = draggedActivity?.position ?? 1
  const label = `移动 ${draggedActivity?.name || 'activity'}`

  setLocalOverrides(prev => ({
    ...prev,
    [activityId]: { day_id: toDayId, position: toPosition }
  }))

  router.patch(
    `/activities/${activityId}/position`,
    { to_day_id: toDayId, to_position: toPosition },
    {
      preserveState: true,
      preserveScroll: true,
      only: [ 'activities', 'violations' ],
      onSuccess: () => {
        setLocalOverrides(prev => {
          const next = { ...prev }
          delete next[activityId]
          return next
        })
        undoStack.push({
          label,
          undoFn: () => new Promise((resolve, reject) =>
            router.patch(`/activities/${activityId}/position`,
              { to_day_id: prevDayId, to_position: prevPosition },
              {
                preserveState: true,
                only: [ 'activities', 'violations' ],
                onSuccess: () => resolve(),
                onError: () => reject(new Error('服务器拒绝'))
              }
            )
          )
        })
      },
      onError: () => {
        setLocalOverrides(prev => {
          const next = { ...prev }
          delete next[activityId]
          return next
        })
        notifications.show({ message: '拖拽未保存，请重试', color: 'red' })
      }
    }
  )
}
```

- [ ] **Step 2: 调整 Show.test.jsx 让 useUndoStack 可被 import**

Show.test.jsx 中需要 mock useUndoStack 因为单独 render Show 不带 Provider：

```jsx
const mockUndoStack = { push: vi.fn(), executeTop: vi.fn(), stack: [] }
vi.mock('../../../hooks/useUndoStack', () => ({
  useUndoStack: () => mockUndoStack,
  UndoStackProvider: ({ children }) => children,
  UNDO_CAP: 10,
}))
```

加在文件 mock 区。

Run: `npm test`

Expected: 全 PASS

### 6B · ActivityDrawer

- [ ] **Step 3: ActivityDrawer create 改 fetch + 3 处 push undo**

读 `app/javascript/components/activity-editor/ActivityDrawer.jsx`。当前 handleSave 用 router.post / router.patch，handleDelete 用 router.delete。

加 import：

```jsx
import { useUndoStack } from '../../hooks/useUndoStack'
```

组件内：

```jsx
const undoStack = useUndoStack()
```

定义辅助 csrfToken helper（如不在已有共享处）：

```jsx
function csrfToken() {
  return document.querySelector('meta[name=csrf-token]')?.getAttribute('content') || ''
}
```

handleSave 改写（差异：create 路径用 fetch 拿 id 后 push undo；update 路径快照 prevAttrs 后 push undo）：

```jsx
const handleSave = async () => {
  if (form.validate().hasErrors) return
  setSaving(true)

  const kind = form.values.kind
  const validKeys = (KIND_SCHEMA[kind] || []).map(f => f.key)
  const cleanDetails = {}
  for (const k of validKeys) {
    if (details[k] !== undefined && details[k] !== '' && details[k] !== null) {
      cleanDetails[k] = details[k]
    }
  }

  const payload = {
    activity: {
      ...form.values,
      planned_duration_min: form.values.planned_duration_min === '' ? null : Number(form.values.planned_duration_min),
      lat: form.values.lat === '' ? null : Number(form.values.lat),
      lng: form.values.lng === '' ? null : Number(form.values.lng),
      details: cleanDetails,
    },
  }

  if (isEdit) {
    // UPDATE path: snapshot prev for undo
    const prevAttrs = { ...activity }
    router.patch(`/activities/${activity.id}`, payload, {
      preserveScroll: true,
      only: ['activities', 'violations'],
      onSuccess: () => {
        setSaving(false)
        onClose()
        undoStack.push({
          label: `修改 ${form.values.name}`,
          undoFn: () => new Promise((resolve, reject) =>
            router.patch(`/activities/${activity.id}`, { activity: prevAttrs }, {
              preserveScroll: true,
              only: ['activities', 'violations'],
              onSuccess: () => resolve(),
              onError: () => reject(new Error('服务器拒绝'))
            })
          )
        })
      },
      onError: () => setSaving(false),
    })
  } else {
    // CREATE path: fetch to get id, then push delete-undo
    const url = targetDayId
      ? `/tours/${tourId}/days/${targetDayId}/activities`
      : `/tours/${tourId}/backlog_activities`
    try {
      const res = await fetch(url, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'Accept': 'application/json', 'X-CSRF-Token': csrfToken() },
        body: JSON.stringify(payload)
      })
      if (!res.ok) throw new Error(`HTTP ${res.status}`)
      const { id: newId } = await res.json()
      router.reload({ only: ['activities', 'violations'] })
      setSaving(false)
      onClose()
      undoStack.push({
        label: `新建 ${form.values.name}`,
        undoFn: () => fetch(`/activities/${newId}`, {
          method: 'DELETE',
          headers: { 'Accept': 'application/json', 'X-CSRF-Token': csrfToken() }
        }).then(r => {
          if (!r.ok) throw new Error('删除失败')
          router.reload({ only: ['activities', 'violations'] })
        })
      })
    } catch (err) {
      setSaving(false)
      notifications.show({ message: `保存失败：${err.message}`, color: 'red' })
    }
  }
}
```

确认 `notifications` 已 import 自 `@mantine/notifications`。如未，添加 import。

handleDelete 改写：

```jsx
const handleDelete = () => {
  modals.openConfirmModal({
    title: '确认删除此 activity？',
    labels: { confirm: '删除', cancel: '取消' },
    confirmProps: { color: 'red' },
    onConfirm: () => {
      const savedAttrs = { ...activity }  // snapshot for restore
      const wasInDay = activity.day_id
      router.delete(`/activities/${activity.id}`, {
        preserveScroll: true,
        only: ['activities', 'violations'],
        onSuccess: () => {
          onClose()
          undoStack.push({
            label: `删除 ${activity.name}`,
            undoFn: async () => {
              // Reverse: POST recreate activity with savedAttrs (gets new id)
              const url = wasInDay
                ? `/tours/${tourId}/days/${wasInDay}/activities`
                : `/tours/${tourId}/backlog_activities`
              const payload = {
                activity: {
                  name: savedAttrs.name,
                  kind: savedAttrs.kind,
                  citizen_level: savedAttrs.citizen_level,
                  lat: savedAttrs.lat,
                  lng: savedAttrs.lng,
                  planned_start_at: savedAttrs.planned_start_at,
                  planned_duration_min: savedAttrs.planned_duration_min,
                  details: savedAttrs.details || {}
                }
              }
              const res = await fetch(url, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json', 'Accept': 'application/json', 'X-CSRF-Token': csrfToken() },
                body: JSON.stringify(payload)
              })
              if (!res.ok) throw new Error(`HTTP ${res.status}`)
              router.reload({ only: ['activities', 'violations'] })
            }
          })
        },
      })
    },
  })
}
```

- [ ] **Step 4: ActivityDrawer.test.jsx 调整 + push 断言**

ActivityDrawer.test.jsx 已 mock `@inertiajs/react` (router.post/patch/delete)。新增需求：mock `useUndoStack` + 全局 `fetch`。

在文件 mock 区添加：

```jsx
const mockUndoStack = { push: vi.fn(), executeTop: vi.fn(), stack: [] }
vi.mock('../../../hooks/useUndoStack', () => ({
  useUndoStack: () => mockUndoStack,
  UndoStackProvider: ({ children }) => children,
  UNDO_CAP: 10,
}))

global.fetch = vi.fn()
```

beforeEach 加 reset：

```jsx
beforeEach(() => {
  mockUndoStack.push.mockClear()
  global.fetch.mockClear()
})
```

由于 create 路径改用 fetch，原"calls router.post for backlog create"测试需要改成"calls fetch for backlog create"：

```jsx
test('calls fetch for backlog create (no targetDayId)', async () => {
  const onClose = vi.fn()
  global.fetch.mockResolvedValue({
    ok: true,
    json: () => Promise.resolve({ id: 999, position: 1 })
  })
  renderDrawer({ onClose })
  fireEvent.change(screen.getByLabelText('名称'), { target: { value: '测试景点' } })
  fireEvent.click(screen.getByRole('button', { name: '保存' }))
  await waitFor(() => {
    expect(global.fetch).toHaveBeenCalledWith(
      '/tours/1/backlog_activities',
      expect.objectContaining({
        method: 'POST',
        headers: expect.objectContaining({ 'Content-Type': 'application/json' })
      })
    )
    expect(onClose).toHaveBeenCalled()
    expect(mockUndoStack.push).toHaveBeenCalledWith(
      expect.objectContaining({ label: expect.stringContaining('测试景点') })
    )
  })
})
```

类似改 day-create 测试用 fetch。

加新测试 `update 路径 push undo`：

```jsx
test('update path pushes undo entry on save success', async () => {
  const onClose = vi.fn()
  const { router } = await import('@inertiajs/react')
  router.patch.mockImplementation((url, data, opts) => opts?.onSuccess?.())
  renderDrawer({
    mode: 'edit',
    activity: { id: 42, name: 'X', kind: 'scenic', citizen_level: 'tier_one', day_id: 5, details: {} },
    onClose,
  })
  fireEvent.change(screen.getByLabelText('名称'), { target: { value: 'X 改' } })
  fireEvent.click(screen.getByRole('button', { name: '保存' }))
  await waitFor(() => {
    expect(mockUndoStack.push).toHaveBeenCalledWith(
      expect.objectContaining({ label: expect.stringContaining('X 改') })
    )
  })
})
```

- [ ] **Step 5: 跑 ActivityDrawer 测试**

Run: `npx vitest run app/javascript/components/activity-editor/__tests__/ActivityDrawer.test.jsx`

Expected: 全 PASS

### 6C · DayEditModal

- [ ] **Step 6: DayEditModal update 处 push undo**

读 `app/javascript/components/planner/DayEditModal.jsx`。在 imports 加：

```jsx
import { useUndoStack } from '../../hooks/useUndoStack'
```

在组件函数内：

```jsx
const undoStack = useUndoStack()
```

修改 handleSave，在调用 router.patch 之前快照 prevAttrs，在 onSuccess 内 push undo：

```jsx
const handleSave = () => {
  if (!day) return
  // Snapshot prev for undo
  const prevAttrs = {
    theme: day.theme,
    date: day.date,
    buffer_day: day.buffer_day,
  }
  setSaving(true)

  router.patch(`/tours/${tourId}/days/${day.id}`, {
    day: { theme: themeValue, date: formatDate(dateValue), buffer_day: bufferValue }
  }, {
    preserveScroll: true,
    only: ['days', 'activities', 'violations'],
    onSuccess: () => {
      setSaving(false)
      onClose()
      undoStack.push({
        label: `修改 D${day.day_index}`,
        undoFn: () => new Promise((resolve, reject) =>
          router.patch(`/tours/${tourId}/days/${day.id}`, { day: prevAttrs }, {
            preserveScroll: true,
            only: ['days', 'activities', 'violations'],
            onSuccess: () => resolve(),
            onError: () => reject(new Error('服务器拒绝'))
          })
        )
      })
    },
    onError: () => setSaving(false),
  })
}
```

如果 DayEditModal 现有 handleSave 字段名与上面不一致（比如 `themeValue` 实际叫 `theme`），保留原命名只是结构对齐。

handleDelete **不**接 undo（per spec），保持现有逻辑。

- [ ] **Step 7: DayEditModal.test.jsx 加 push 断言**

mock useUndoStack（同 6B）：

```jsx
const mockUndoStack = { push: vi.fn(), executeTop: vi.fn(), stack: [] }
vi.mock('../../../hooks/useUndoStack', () => ({
  useUndoStack: () => mockUndoStack,
  UndoStackProvider: ({ children }) => children,
  UNDO_CAP: 10,
}))

beforeEach(() => {
  mockUndoStack.push.mockClear()
})
```

加测试：

```jsx
test('save pushes undo entry with prev attrs', async () => {
  const { router } = await import('@inertiajs/react')
  router.patch.mockImplementation((url, data, opts) => opts?.onSuccess?.())
  renderModal({
    day: { id: 10, day_index: 3, theme: '原主题', date: '2026-06-12', buffer_day: false }
  })
  fireEvent.click(screen.getByRole('button', { name: '保存' }))
  await waitFor(() => {
    expect(mockUndoStack.push).toHaveBeenCalledWith(
      expect.objectContaining({ label: expect.stringContaining('D3') })
    )
  })
})
```

Run: `npx vitest run app/javascript/components/planner/__tests__/DayEditModal.test.jsx`

Expected: 全 PASS

- [ ] **Step 8: 全量 Vitest + RSpec**

Run: `npm test && mise exec -- bundle exec rspec --format progress`

Expected: 全 PASS

- [ ] **Step 9: Commit**

```bash
git add app/javascript/pages/Tour/Show.jsx app/javascript/pages/Tour/__tests__/Show.test.jsx app/javascript/components/activity-editor/ActivityDrawer.jsx app/javascript/components/activity-editor/__tests__/ActivityDrawer.test.jsx app/javascript/components/planner/DayEditModal.jsx app/javascript/components/planner/__tests__/DayEditModal.test.jsx
git commit -m "undo: wire 5 mutation paths (drag, activity create/update/delete, day update)"
```

---

## Task 7: 最终验证 + 浏览器手动 QA

- [ ] **Step 1: 全量 Vitest**

Run: `npm test`

Expected: ALL PASS（基线 ~136 → +12 ≈ 148）

- [ ] **Step 2: 全量 RSpec**

Run: `mise exec -- bundle exec rspec --format progress`

Expected: ALL PASS（基线 ~251 → +5 ≈ 256）

- [ ] **Step 3: RuboCop**

Run: `mise exec -- bundle exec rubocop -f github`

Expected: 没有新 offense（已知有 5 条 pre-existing）

- [ ] **Step 4: 浏览器手测 — Tour #17 完整 QA**

启动 dev server（如未起），登录 `d1-pm@example.com`，访问 `/tours/17`。

| # | 期望 |
|---|---|
| 1 | 页面打开后 Chat 显示历史 messages（不再"还没有对话"）— **Ops-A 验证** |
| 2 | 拖一个 6h 的 activity 到 D2（已有 6h+ 驾驶）：D2 立即变红框 + 显示"⚠ 加入后驾驶 720/420 min" — **G5-a 验证** |
| 3 | 拖到 D6（buffer day）：弹 modal "把 activity 放进机动日？继续放入会让 D6 不再是机动日，确认吗？" — **G5-b 验证** |
| 4 | confirm → D6 buffer_day 改 false（footer 不再有"buffer"标记）+ activity 落到 D6 — **G5-b 验证** |
| 5 | 任一 activity 拖到别处后 toast 出现 "移动 X" + "撤销" 按钮 — **undo 验证** |
| 6 | 点击 toast 内 "撤销"：activity 自动回到原位 — **undo 验证** |
| 7 | 编辑 ActivityDrawer 改名后保存 → toast "修改 X" + Cmd+Z 能撤销回原名 — **undo 验证** |
| 8 | 删除 activity → toast "删除 X" + 撤销恢复（id 不同但内容一致）— **undo 验证** |
| 9 | 创建新 activity → toast "新建 X" + 撤销直接删除 — **undo 验证** |
| 10 | 编辑 DayEditModal 改 theme 保存 → toast "修改 D2" + 撤销 — **undo 验证** |
| 11 | DayEditModal 删除 day → 不弹 undo toast（per spec）— **验证 day delete 不入 stack** |
| 12 | 触发 11 次 mutation：第 1 次的 toast 已消失但 stack 已 shift（无可视化方式直接验证，靠 Cmd+Z 11 次能撤回多少 = 10 次）|
| 13 | 在文本框焦点时按 Cmd+Z：原生 undo 生效，不触发 stack — **验证不冲突** |
| 14 | 控制台无 error |

- [ ] **Step 5: 若有 fixes 最后 commit**

```bash
git add -A
git commit -m "chore: D-2 final fixes"
```

---

## 附：实施注意事项

- **Mantine notifications 的 `message: ReactNode`** — 已经在 useUndoStack 内定义 toast 含 button。不要把 toast 作为 string。
- **fetch CSRF token** — Rails 默认 CSRF 保护。新建/删除走 fetch 时必须带 `X-CSRF-Token` header（从 `meta[name=csrf-token]` 读）。已在 useChat / Plan 内有示例。
- **router.reload after fetch** — fetch POST/DELETE 拿到响应后必须 `router.reload({ only: ['activities', 'violations'] })`，否则前端 props 不刷新。
- **prevAttrs 浅拷贝** — Activity 的 `details` 是 hash，浅拷贝足够（更新时整个 details 替换）。Day 同理。
- **activity create 的 fetch 路径与 controller 行为** — 后端 controller 已改为 `respond_to`：`Accept: application/json` 走 JSON，否则 redirect。前端 Inertia 默认 Accept 是 `text/html, application/xhtml+xml, ...`，所以 router.post 仍走 redirect 路径——但本 task 用 fetch 替代 router.post for create，需主动设 `Accept: application/json`。
- **测试中 mock useUndoStack** — 任何使用 useUndoStack 的组件（Show.jsx / ActivityDrawer / DayEditModal）的 test.jsx 都需 mock 这个 hook，否则 Provider 缺失会抛 "useUndoStack must be used within UndoStackProvider"。
- **回滚**：每 task 一个独立 commit；任何 step 失败可单独 `git revert <sha>`。

---

## 数字预估

| | 数量 |
|---|---|
| 新文件 | 3 |
| 修改文件（前端） | 7 |
| 修改文件（后端） | 4 |
| Vitest 新增 | ~12 |
| RSpec 新增 | ~5（含 message_spec 2 + activities/days create JSON 3）|
| Commits | 6 + 1 final = 7 |
| **工作量** | **~12-16h** |
