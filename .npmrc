# vite-plugin-pwa@1.2.0 peer 还卡 vite@^7,但 project 是 vite@8。实际兼容
# (PWA E2E 全验过),只是 strict 拒。让 npm ci / npm i 都走 legacy-peer-deps,
# 跟本地装的方式对齐。等 vite-plugin-pwa 出支持 vite@8 的版本后可以删。
legacy-peer-deps=true
